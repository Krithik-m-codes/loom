[package]
name = "engine-goose"
version = "0.1.0"
edition = "2021"
description = "Goose engine adapter for Loom — subprocess-only, stdout parsing"

[dependencies]
engine-core = { path = "../engine-core" }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
async-trait = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
which = "7"
async-stream = "0.3"
futures = "0.3"
regex = "1"
